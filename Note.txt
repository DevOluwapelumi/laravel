
    // To check the route ---
    php artisan make:model Student -m

    // To Create model ---
    php artisan make:model Student -m

    // To Create Conroller ---
    php artisan make:controller StudentController --resource

    composer require laravel/ui
    php artisan ui bootstrap --auth

    // To add column to a table ---
    php artisan make:migration add_user_id_to_noteapp_table_table

    after the above column migrate --- 
    php artisan migrate

    // To Create a MiddleWare //
    php artisan make:middleware NoteappMiddleware